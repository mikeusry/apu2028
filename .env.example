# Cloudinary Configuration
# Cloud Name (public - safe to expose in client)
PUBLIC_CLOUDINARY_CLOUD_NAME=your-cloud-name

# API credentials (private - server-side only, for uploads)
CLOUDINARY_API_KEY=your-api-key
CLOUDINARY_API_SECRET=your-api-secret

# Folder where images are stored
CLOUDINARY_FOLDER=AynParkerUsry

# ============================================
# Recruitment Tracker
# ============================================

# Vercel KV (Redis) - Auto-configured by Vercel when you add KV storage
# These are automatically set when you link a KV database in Vercel
KV_URL=
KV_REST_API_URL=
KV_REST_API_TOKEN=
KV_REST_API_READ_ONLY_TOKEN=

# Dashboard Access Key - Change this to something secure!
TRACKER_VIEW_KEY=ayn2028

# ============================================
# Email Alerts (SendGrid)
# ============================================

# SendGrid API Key (from https://app.sendgrid.com/settings/api_keys)
SENDGRID_API_KEY=

# From email (must be verified sender in SendGrid)
SENDGRID_FROM_EMAIL=alerts@point.dog

# Where to send alerts
ALERT_EMAIL=mike@point.dog
